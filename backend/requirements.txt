# --- Runtime (required to run the API) ---
fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
motor==3.3.1
pymongo==4.5.0
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
bcrypt==4.1.3
tzdata>=2024.2
python-multipart>=0.0.9

# --- Tests (safe to keep; harmless in prod) ---
requests>=2.31.0
pytest>=8.0.0

# Removed for the Emergent->self-host migration (unused / Emergent-only):
#   emergentintegrations (Emergent-only, won't install elsewhere),
#   boto3, pandas, numpy, jq, typer, passlib, python-jose,
#   requests-oauthlib, cryptography, black, isort, flake8, mypy
